import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.apps.testDany',
  appName: 'newApp2',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
